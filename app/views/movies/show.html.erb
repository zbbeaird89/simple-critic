<%= render "movies/movie_info" %>

<%= link_to "Add Review", new_movie_review_path(@movie), class: "btn btn-lg review-btn mb-5 mt-3" %>

<% @reviews.each do |review| %>
  <%= render "shared/cards/review_card", review: review, user: current_user %>
<% end %>
